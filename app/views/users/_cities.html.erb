<%= label_tag "city_id" %>
<% if !@cities.blank? %>
      <%= select("user", "city_id", @cities.collect {|p| [ p.name, p.id ] }, { :prompt => "Select a City" }) %>
<% else %>
      <%= select("user", "city_id", [], { :prompt => "Select a City" }) %>
<% end %>
