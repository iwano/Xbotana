<% provide(:title, 'All users') %>

<h1>All users</h1>
<ul data-role="listview" data-theme="g">
	<% @users.each do |user|%>
	<li>
  		<%= gravatar_for user, size: 50 %>
  		<%= link_to user.name, user, :data => { :transition => 'slide' } %>
	</li>
	<% end %>
</ul>