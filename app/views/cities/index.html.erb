<% provide(:title, 'All Cities') %>

<%= link_to '+ Add', 'cities/new', class: 'btn btn-success', style: 'float:left'%>
<h1>All Cities</h1>

<%= will_paginate %>
    <%= render "cities_index" %>
<%= will_paginate %>