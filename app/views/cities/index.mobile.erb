<% provide(:title, 'All Cities') %>
<h1>All Cities</h1>
<ul data-role="listview" data-theme="c">
<li data-role="list-divider"></li>
	<% @cities.each do |city| %>
		<li>
			<%= link_to city.name, city, :data => { :transition => 'slide' }%>
		</li>
	<% end %>
</ul>