<table class='table-striped table'>  
	 <tr>
		<th>Name</th>
	    <th></th>
	    <th></th>
	    <th></th>

	 </tr>

	<% @cities.each do |city| %>

	<tr>
		<td><h4><%= link_to city.name, city%></h4></td>	
		<td><%= link_to 'Show', city, class: 'btn btn-primary' %></td>
		<td><%= link_to 'Edit', edit_city_path(city), class: 'btn btn-warning'%></td>
		<td><%= link_to 'Delete', city, method: :delete, confirm: 'Are you sure?', class: 'btn btn-danger'%></td>
	</tr>
 	<% end %>

</table>  
<br />