<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
    <style>
    header#main{
    color: white;
    background: #2C2C2C;
    background-image:-webkit-gradient(linear, 0 0, 0 100%, from(#333333), to(#222222));
    background-image: -moz-linear-gradient(top, #333333, #222222);
    background-image: -o-linear-gradient(top, #333333, #222222);
    background-image: linear-gradient(top, #333333, #222222);
    height:40px;
    display:block;
  }
  h1#logo{
    color:white;
    float:right;
    font-size:22px;
    font-weight:bold;
    letter-spacing: -1px;
    font-family: 'Helvetica Neue', "MS Trebuchet", sans-serif;
    padding-right:15px;
  }
table{
  border-collapse:collapse;
}
table tr td{
  border: 1px solid #000;
 }
.head{
  font-weight: bold;
}
</style>
  </head>
  <body>
    <header id="main"><h1 id="logo">botanaX</h1></header>
    <h1>Theres a new order that needs to be delivered</h1>
    <br />

      <table> 
      <tr>
        <td class="head">User: </td>
        <td><%= @user.name %></td>
      </tr>
      <tr>
        <td class="head">State: </td>
        <td><%= @user.state.name %></td>
      </tr>
      <tr>
        <td class="head">City: </td>
        <td><%= @user.city.name %></td>
      </tr>
      <tr>
        <td class="head">No. prods: </td>
        <td><%= @route_detail.order.number_products %></td>
      </tr>
      <tr>
        <td class="head">Total: </td>
        <td><%= number_to_currency (@route_detail.order.total) %></td>
      </tr>
      <tr>
        <td class="head">Route no.: </td>
        <td><%= @route_detail.route.id %></td>
      </tr>
    </table>

    <p>
      To see the order details or your routes, just follow this link: <%= @url %>.
    </p>
  </body>
</html>