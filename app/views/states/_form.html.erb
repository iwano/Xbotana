<%= form_for(@state) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>

	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.submit "Add", class: "btn btn-large btn-primary" %>
	<%= link_to "Cancel", states_path, class: "btn" %>
	
<% end %>