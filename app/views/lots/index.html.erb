<% provide(:title, 'All Lots') %>
<div class='span8'>
<%= link_to '+ Add', 'lots/new', class: 'btn btn-success', style: 'float:left'%>
<h1>All Lots</h1>

<%= will_paginate %>
	<table class='table-striped table'>  
		 <tr>
			<th>Name</th>
		    <th>Expiry Date</th>
		    <th></th>
		    <th></th>
			<th></th>   
		 </tr>

		<% @lots.each do |lot| %>

		<tr>
			<td><h4><%= link_to lot.number, lot%></h4></td>	
			<td><h4><%= lot.expiry_date %></h4></td>
			<td><%= link_to 'Show', lot, class: 'btn btn-primary' %></td>
			<td><%= link_to 'Edit', edit_lot_path(lot), class: 'btn btn-warning'%></td>
			<td><%= link_to 'Delete', lot, method: :delete, confirm: 'Are you sure?', class: 'btn btn-danger'%></td>
		</tr>
	 	<% end %>

	</table>  
	<br />

<%= will_paginate %>
</div>


